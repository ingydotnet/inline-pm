Ken Williams wrote:

> Because the '$VERSION = '0.20' is happening at runtime, but the 'use
> Inline...' is happening at compile time.  So $VERSION isn't set yet when
> you pass it to Inline.

Oh, Duh, Duh, Duh!

Thanks.  Talk about missing the bleedin' obvious...

I've noticed that Inline::MakeMaker seems a bit broken, for example if I specify
'LIBS => [ ... ]' in the WriteInlineMakefile call it is ignored.  Is this a
known problem?

-- 
Alan Burlison
--
$ head -1 /dev/bollocks
proactively refactor performant interconnected channels, going forwards
